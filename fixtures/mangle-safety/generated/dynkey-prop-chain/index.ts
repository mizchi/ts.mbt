// GENERATED by scripts/generate_mangle_cases.mjs — do not edit.
//
// Reach:    a computed key whose value is the string "alphaTop".
// Receiver: a property chain read from outside the class.
//
// Derived expectation: keep [alphaTop]. The name is a static key at
// the declaration and a string VALUE at the read, so renaming one
// without the other turns `echoed` into `undefined`. Nothing is
// manglable: the key set behind a computed read is unbounded, so
// the reservation is the wildcard and omegaCtl rides along.

class Holder {
  bag: { alphaTop: number } = { alphaTop: 1 };
}
const holder = new Holder();
function pick(k: string): number | undefined {
  return (holder.bag as any)[k];
}

export const echoed = pick("alphaTop");

// The control: same kind of value, never reached dynamically.
const control = { omegaCtl: 7 };
export const total = control.omegaCtl;
