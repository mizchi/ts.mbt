{
  "origin": "packelyze fixtures/case19-extend",
  "what": "Object spread widening a base shape into an intersection type, both of them exported.",
  "exports": [
    "extend"
  ],
  "expectKeep": [
    "fileName",
    "original",
    "to"
  ],
  "expectStatus": "blocked-compile",
  "blockedReason": "checker: `mtsc` type-checks each file alone, so an imported `type Loc` is unresolvable and an identical inferred shape reads as a mismatch. `@mtsc.check_module_graph` resolves the graph; the CLI does not use it yet."
}
