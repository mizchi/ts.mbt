// Sub-module that owns the literal-union aliases driving the
// cartesian. Mirrors the zod `util.d.ts` shape.
export type HashAlgorithm = "md5" | "sha1" | "sha256";
export type HashEncoding = "hex" | "base64";
