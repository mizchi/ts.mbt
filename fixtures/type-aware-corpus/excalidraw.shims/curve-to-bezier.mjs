// `points-on-curve/lib/curve-to-bezier` — the same missing-extension
// problem as the roughjs subpaths, but only that: the real module loads
// once the extension is supplied.
export { curveToBezier } from "points-on-curve/lib/curve-to-bezier.js";
